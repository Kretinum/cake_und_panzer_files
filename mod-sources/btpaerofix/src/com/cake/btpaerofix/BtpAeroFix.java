package com.cake.btpaerofix;

import net.neoforged.fml.common.Mod;

/**
 * Marker mod so the modId "btpaerofix" is backed by a class.
 * All real work happens in the client-only mixin.
 */
@Mod("btpaerofix")
public class BtpAeroFix {
    public BtpAeroFix() {
    }
}
