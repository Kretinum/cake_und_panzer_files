package com.cake.graveyardfix;

import net.neoforged.fml.common.Mod;

/**
 * Marker mod so the modId "graveyardfix" is backed by a class.
 * All real work happens in the client-only mixin.
 */
@Mod("graveyardfix")
public class GraveyardFix {
    public GraveyardFix() {
    }
}
