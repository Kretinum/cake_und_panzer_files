package com.cake.noglint;

import net.neoforged.fml.common.Mod;

/**
 * Marker mod so the modId "noenchantglint" is backed by a class.
 * All work happens in the client-only mixin.
 */
@Mod("noenchantglint")
public class NoGlint {
    public NoGlint() {
    }
}
